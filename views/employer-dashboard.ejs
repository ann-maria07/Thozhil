<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>Employer Dashboard</title>
  <style>
    /* Center the content vertically and horizontally */
    body {
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      height: 100vh;
      margin: 0;
      background-color: #f0f0f0;
      background-image: url("images/backg.jpeg");

    }

    /* Styling for the employer dashboard container */
    .dashboard-container {
      padding: 30px;
      border: 2px solid #333;
      border-radius: 10px;
      background-color: rgba(255, 255, 255, 0.8); /* Set a higher alpha value for less transparency */
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.2);
      text-align: center;
      max-width: 500px;
    }

    /* Styling for the employer name heading */
    .dashboard-title {
      font-size: 28px;
      margin-bottom: 20px;
      color: #333;
    }

    /* Styling for the buttons */
    .dashboard-buttons {
      display: flex;
      justify-content: center;
      margin-top: 20px;
    }

    .dashboard-button {
      display: inline-block;
      padding: 12px 24px;
      margin: 10px;
      background-color: #0c0c0c;
      color: #fff;
      border: none;
      border-radius: 8px;
      cursor: pointer;
      font-size: 16px;
      text-decoration: none;
      transition: background-color 0.4s ease, box-shadow 0.4s ease;
    }

    .dashboard-button:hover {
      background-color: #272829;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.3);
    }

    /* Additional styling for the button when active (clicking) */
    .dashboard-button:active {
      background-color: #0c0c0c;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.6);
    }

    /* Logout button styles */
    .logout-button {
      position: absolute;
      top: 20px;
      right: 20px;
      background-color: #000000;
      color: #fff;
      padding: 10px 20px;
      border-radius: 5px;
      text-decoration: none;
      font-size: 16px;
      font-weight: bold;
      transition: background-color 0.3s;
    }

    .logout-button:hover {
      background-color: #2c2c2c;
    }
  </style>
</head>
<body>
  </style>
</head>
<body>
  <div class="dashboard-container">
    <a href="/index" class="logout-button">Logout</a>
    <h1 class="dashboard-title">Welcome <%= employer.name %> </h1>
    <!-- Display employer-specific information here -->
    <!-- Add other employer-specific details as needed -->

    <!-- Buttons -->
    <div class="dashboard-buttons">
      <button class="dashboard-button" onclick="location.href='/employerpost.html'">Post a Job</button>
      <button class="dashboard-button" onclick="location.href='/empupdate'">Update Job</button>
    </div>
  </div>
</body>
</html>
