<!DOCTYPE html>
<html lang="en">
<head>
  <link rel="stylesheet" href="/styles.css"> 
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Employee Module</title>
  <style>
    /* Center the content vertically and horizontally */
    body {
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      height: 100vh;
      margin: 0;
      background-image: url("images/backg.jpeg");
      background-color: #f0f0f0;
      position: relative; /* Added this line */
    }

    /* Styling for the "Welcome" heading */
    h1 {
      text-align: center;
      font-size: 58px;
      margin-bottom: 50px;
      color: #333;
      position: absolute; /* Added this line */
      top: 7%; /* Added this line */
      left: 50%; /* Added this line */
      transform: translateX(-50%); /* Added this line */
    }

    /* Styling for the buttons */
    .login-buttons {
      display: flex;
      flex-direction: column;
      align-items: center;
    }

    .login-button {
      display: inline-block;
      padding: 15px 30px;
      margin: 10px;
      background-color: #0c0c0c;
      color: #fff;
      border: none;
      border-radius: 8px;
      cursor: pointer;
      font-size: 18px;
      text-decoration: none;
      transition: background-color 0.4s ease, box-shadow 0.4s ease;
    }

    .login-button:hover {
      background-color: #272829;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.3);
    }

    /* Additional styling for the button when active (clicking) */
    .login-button:active {
      background-color: #0c0c0c;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.6);
    }
  </style>
</head>
<body>
  <h1>Welcome</h1>
  <div class="login-buttons">
    <a href="/login" class="login-button">Login as Employee</a>
    <a href="/login-employer" class="login-button" onclick="loginAsEmployer()">Login as Employer</a>
  </div>

  <script>
    function loginAsEmployer() {
      // Redirect to the login page for employers
      window.location.href = "/login-employer";
    }
  </script>
  
</body>
</html>
